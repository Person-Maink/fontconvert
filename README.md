# Font Convert

A simple command-line interface (CLI) for converting font data.

## Installation

To install the dependencies for this project, use:
```
pip install -r requirements.txt
```

## Usage

You can use the CLI by running:
```
python -m src.fontconvert
```

## Building

To build the font files, run:
```
python build.py
```
